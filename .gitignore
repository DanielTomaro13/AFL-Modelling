# Not part of this repo
engine_ref/
**/engine_ref/
*.cs
*.csproj
*.sln
*.csx

# Python
.venv/
.venv-afl/
__pycache__/
*.pyc
*.pyo

# Raw cached feeds (rebuilt by ingest; large) — keep processed parquet only
data/raw/

# Secrets / tokens — NEVER commit (use env vars or GitHub secrets)
.env
.env.*
!.env.example
*.token
tokens.json

# macOS / editor
.DS_Store
**/.DS_Store
.claude/

# Large derived feature matrix — regenerated by features.py, not committed
data/processed/features.parquet
