# Pinned so the env that PICKLES models/afl_models.joblib (model.yml) and the env
# that LOADS it (predict.yml) can never drift. An sklearn upgrade against a
# committed pickle previously broke predict with "No module named '_loss'".
pandas==3.0.3
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
pyarrow==24.0.0
